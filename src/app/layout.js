import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Webixo - Website Development, Digital Marketing & Mobile App Experts",
  description:
    "Boost your business with Webixo! We offer SEO-optimized websites, high-ROI digital marketing, and scalable mobile app development. Get a high-performance online presence today! ✅ Contact us now",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
